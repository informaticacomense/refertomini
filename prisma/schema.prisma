// ======================================================
// REFERTIMINI – powered by Informatica Comense
// Database schema: referti-minibasket
// ======================================================

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum GameStatus {
  IN_PROGRAMMA
  CARICATA
  OMOLOGATA
}

// ======================================================
// MODELLO STAGIONE SPORTIVA
// ======================================================
model Season {
  id         String      @id @default(cuid())
  name       String      @unique                     // es: "2025/2026"
  startDate  DateTime
  endDate    DateTime
  committees Committee[]
  categories Category[]
  createdAt  DateTime    @default(now())
}

// ======================================================
// MODELLO COMITATO (es. FIP ComoLecco)
// ======================================================
model Committee {
  id         String      @id @default(cuid())
  name       String
  logoUrl    String?
  seasonId   String
  season     Season      @relation(fields: [seasonId], references: [id])
  categories Category[]
  companies  Company[]
  users      User[]
  createdAt  DateTime    @default(now())

  @@unique([seasonId, name])
}

// ======================================================
// MODELLO CATEGORIA (es. Scoiattoli Maschile / Gazzelle Femminile…)
// ======================================================
model Category {
  id          String      @id @default(cuid())
  name        String                               // Nome completo (es. Aquilotti Maschile)
  shortName   String                               // Nome breve (es. AQM)
  gender      String                               // Maschile / Femminile
  seasonId    String
  season      Season     @relation(fields: [seasonId], references: [id])
  committeeId String
  committee   Committee  @relation(fields: [committeeId], references: [id])
  groups      Group[]
  phases      Phase[]
  games       Game[]
  createdAt   DateTime   @default(now())

  @@unique([committeeId, name, seasonId])
}

// ======================================================
// MODELLO GIRONE (es. Girone A, Girone B…)
// ======================================================
model Group {
  id          String     @id @default(cuid())
  name        String
  categoryId  String
  category    Category   @relation(fields: [categoryId], references: [id])
  createdAt   DateTime   @default(now())
}

// ======================================================
// MODELLO FASE (es. Fase Provinciale, Fase Regionale…)
// ======================================================
model Phase {
  id          String     @id @default(cuid())
  name        String
  categoryId  String
  category    Category   @relation(fields: [categoryId], references: [id])
  createdAt   DateTime   @default(now())
}

// ======================================================
// MODELLO SOCIETÀ / SQUADRA
// ======================================================
model Company {
  id          String      @id @default(cuid())
  name        String
  committeeId String
  committee   Committee   @relation(fields: [committeeId], references: [id])
  users       User[]
  gamesA      Game[]      @relation("TeamA")
  gamesB      Game[]      @relation("TeamB")
  createdAt   DateTime    @default(now())

  @@unique([committeeId, name])
}

// ======================================================
// MODELLO UTENTE
// ======================================================
model User {
  id                String     @id @default(cuid())
  email             String     @unique
  password          String
  firstName         String
  lastName          String
  teamName          String?
  isAdmin           Boolean    @default(false)          // admin del comitato
  isSuperAdmin      Boolean    @default(false)          // super admin globale
  emailVerified     Boolean    @default(false)
  verificationToken String?
  resetToken        String?
  resetTokenExp     DateTime?

  companyId   String?
  company     Company?    @relation(fields: [companyId], references: [id])

  committeeId String?
  committee   Committee?  @relation(fields: [committeeId], references: [id])

  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt

  games       Game[]      @relation("GameOwners")   // gare collegate
  reports     Report[]    @relation("UserReports")  // referti caricati
}

// ======================================================
// MODELLO GARA
// ======================================================
model Game {
  id           String     @id @default(cuid())
  number       String                               // Numero gara (es. "MB1234")
  dayName      String                               // Giorno della settimana
  date         DateTime
  timeStr      String                               // Ora della partita
  venue        String                               // Campo di gioco
  status       GameStatus @default(IN_PROGRAMMA)
  result       String?
  notes        String?

  categoryId   String
  category     Category   @relation(fields: [categoryId], references: [id])

  teamAId      String?
  teamA        Company?   @relation("TeamA", fields: [teamAId], references: [id])
  teamBId      String?
  teamB        Company?   @relation("TeamB", fields: [teamBId], references: [id])

  owners       User[]     @relation("GameOwners")    // utenti che possono modificarla
  report       Report?                              // referto PDF
  createdAt    DateTime   @default(now())
  updatedAt    DateTime   @updatedAt

  @@unique([categoryId, number])
}

// ======================================================
// MODELLO REFERTO PDF
// ======================================================
model Report {
  id           String     @id @default(cuid())
  gameId       String     @unique
  game         Game       @relation(fields: [gameId], references: [id])
  fileKey      String                               // percorso file su disco/S3
  mimeType     String                               // sempre application/pdf
  uploadedById String
  uploadedBy   User       @relation("UserReports", fields: [uploadedById], references: [id])
  createdAt    DateTime   @default(now())
}

// ======================================================
// MODELLO CATEGORIA (es. "Aquilotti", "Esordienti")
// ======================================================
model Category {
  id          String       @id @default(cuid())
  name        String
  seasonId    String
  season      Season       @relation(fields: [seasonId], references: [id])
  committeeId String
  committee   Committee    @relation(fields: [committeeId], references: [id])
  groups      Group[]      // collegamento ai gironi
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
}

// ======================================================
// MODELLO GIRONI (es. "Girone A", "Girone B")
// ======================================================
model Group {
  id          String       @id @default(cuid())
  name        String
  categoryId  String
  category    Category     @relation(fields: [categoryId], references: [id])
  seasonId    String
  season      Season       @relation(fields: [seasonId], references: [id])
  committeeId String
  committee   Committee    @relation(fields: [committeeId], references: [id])
  teams       TeamInGroup[]
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
}

// ======================================================
// MODELLO DI ASSOCIAZIONE SQUADRE-GIRONI
// ======================================================
model TeamInGroup {
  id        String   @id @default(cuid())
  groupId   String
  teamId    String
  group     Group    @relation(fields: [groupId], references: [id])
  team      Team     @relation(fields: [teamId], references: [id])
  createdAt DateTime @default(now())
}

